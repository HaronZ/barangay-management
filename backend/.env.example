# ===========================================
# BARANGAY MANAGEMENT SYSTEM - ENVIRONMENT CONFIG
# ===========================================
# Copy this file to .env and update the values

# ===========================================
# DATABASE
# ===========================================
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/barangay_db?schema=public"

# ===========================================
# JWT CONFIGURATION
# ===========================================
# IMPORTANT: Use a strong secret in production (minimum 32 characters)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="your-super-secret-jwt-key-change-in-production-min-32-chars"
JWT_EXPIRES_IN="7d"

# ===========================================
# SERVER
# ===========================================
PORT=3000
NODE_ENV=development

# ===========================================
# EMAIL - Option 1: Resend (RECOMMENDED)
# ===========================================
# Sign up at https://resend.com (free: 100 emails/day)
# Get your API key from Dashboard > API Keys
RESEND_API_KEY=re_your_api_key_here
EMAIL_FROM="Barangay Management <onboarding@resend.dev>"

# ===========================================
# EMAIL - Option 2: SMTP (Gmail, etc.)
# ===========================================
# For Gmail:
# 1. Enable 2-Factor Authentication on your Google account
# 2. Generate an App Password: https://myaccount.google.com/apppasswords
# 3. Use that 16-character password as SMTP_PASS

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-16-char-app-password
SMTP_FROM="Barangay Management <your-email@gmail.com>"

# ===========================================
# FRONTEND URL
# ===========================================
# Used for email links (verification, password reset)
FRONTEND_URL=http://localhost:5173

# ===========================================
# PRODUCTION CHECKLIST
# ===========================================
# Before deploying to production:
# [ ] Set NODE_ENV=production
# [ ] Use a strong JWT_SECRET (32+ random characters)
# [ ] Configure real SMTP credentials
# [ ] Update FRONTEND_URL to production domain
# [ ] Set up SSL/HTTPS
# [ ] Configure database connection pooling
